# Clean Generated Files

Remove cache and generated files from the project.

## Instructions

Run the clean command:

```bash
just clean
```

This removes `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, and compiled `.pyc` files.

## After Running

1. Report what was cleaned
2. Confirm the project is in a clean state
