# Verify

Run the full verification suite: format, lint, and test.

## Instructions

Run all checks in sequence:

```bash
just format
just lint
just test
```

If any step fails, stop and fix the issues before continuing to the next step.

## After Running

1. Report the results of each step (format, lint, test)
2. If all pass, confirm the project is in a clean state
3. If any fail, offer to fix the issues
