# {{project_name}}

{{description}}

## Quick Reference

| Task | Command |
|------|---------|
| Run tests | `just test` or `/test` |
| Lint code | `just lint` or `/lint` |
| Format code | `just format` or `/format` |
| Fix lint issues | `just fix` |
| **Verify all** | `/verify` (format + lint + test) |
| Install deps | `just install` or `uv sync` |
| Add dependency | `uv add <package>` |
| **Run demo** | `just demo` |
| Run app | `uv run python -m {{package_name}}` |
| Clean files | `just clean` or `/clean` |

## Demo

Run `just demo` to populate fixture data and launch a Streamlit app showcasing all functionality. Every project should maintain a working demo that visually demonstrates what it can do.

The demo:
1. Runs `{{package_name}}.demo --setup` to populate a local data store with fixture data
2. Launches a Streamlit app at `src/{{package_name}}/demo_app.py`

When adding new features, update the demo to showcase them.

## Project Structure

```
{{project_name}}/
├── src/{{package_name}}/    # Main package source code
│   ├── demo.py              # Demo fixture data setup
│   └── demo_app.py          # Streamlit demo app
├── tests/                   # Test files
├── docs/                    # Documentation (MkDocs)
├── .github/                 # GitHub workflows, agents, and scoped instructions
│   ├── agents/              # Copilot agent definitions
│   └── instructions/        # Copilot file-scoped instructions
├── .claude/                 # Claude Code configuration
│   ├── settings.json        # Hooks (auto-format/lint on edit)
│   ├── hooks/               # Hook scripts
│   ├── rules/               # Claude Code file-scoped rules
│   └── commands/            # Claude Code slash commands
├── justfile                 # Task runner (all dev commands)
├── pyproject.toml           # Project configuration
└── CLAUDE.md                # This file (read by both Copilot and Claude Code)
```

## Context Documents

| Document | When to Read |
|----------|--------------|
| `CONTRIBUTING.md` | For contribution guidelines |
| `CHANGELOG.md` | Before adding release notes |
| `docs/docs/developer/architecture.md` | Before making structural changes |
| [acme-arch/docs/acme-architecture.md](https://github.com/blackwhitehere/acme-arch/blob/main/docs/acme-architecture.md) | For overall ACME system architecture |

## Coding Standards

- **Style**: Use ruff for linting and formatting
- **Testing**: All new code requires tests in `tests/`
- **Types**: Include type hints in function signatures
- **Docs**: Keep docstrings concise but informative
- **DRY**: Avoid duplication; centralize shared logic
- **Simplicity**: Prefer simple solutions over complex abstractions

## Workflow Requirements

### Before Committing

1. Run `just lint` - fix any errors
2. Run `just format` - format code
3. Run `just test` - ensure tests pass
4. Update `CHANGELOG.md` under `[Unreleased]`

### Pull Request Checklist

- [ ] Tests added/updated for changes
- [ ] Linting passes (`just lint`)
- [ ] CHANGELOG.md updated
- [ ] Demo updated if new features added
- [ ] Documentation updated if needed

## Agent Configuration

This project uses `CLAUDE.md` as the primary instruction file for both GitHub Copilot and Claude Code. Tool-specific configuration:

| Path | Tool | Purpose |
|------|------|---------|
| `.github/instructions/*.instructions.md` | Copilot | File-scoped rules (Python src, tests, docs) |
| `.github/agents/*.agent.md` | Copilot | Agent definitions (contribution-expert, plan) |
| `.claude/rules/*.md` | Claude Code | File-scoped rules (same content as Copilot) |
| `.claude/commands/*.md` | Claude Code | Slash commands (`/test`, `/lint`, `/format`, `/verify`, `/clean`) |
| `.claude/settings.json` | Claude Code | Hooks: auto-format and lint `.py` files after edits |
| `.claude/hooks/` | Claude Code | Hook scripts executed by settings.json |

## Template Origin

This project was created from the `acme-project-create` template. If you develop reusable patterns or improvements, consider contributing them back to the template.
